dependencies {
	provided group: "commons-cli", name: "commons-cli", version: "1.3.1"
	provided group: "jline", name: "jline", version: "2.12"
}

deploy {
	from ('properties') {
		include 'portal-upgrade.properties'
		include 'server.properties'
	}
}

liferay {
	deployDir = file("${liferayHome}/upgrade-tool")
}